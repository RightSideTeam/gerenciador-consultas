<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>Consultas</title>
</head>
<body>
	<div th:replace="fragmentos/fragmentos.html :: cabecalho"></div>
	<div class="container">
		<h3>Listagem de Consultas</h3>
		<div th:each="consulta: %{consultas}">
			<div class="card bg-light mb-3">
			<div class="card-header" th:text="${consulta.nomePaciente}"></div>
			<div class="card-body">
				<p class="card-text" th:text="${consulta.nomeMedico}"></p>
				<p class="card-text" th:text="${'Data da consulta:'+ #dates.format(consulta.dataConsulta,'dd/MM/yyyy')}"></p>
			
			
			</div>
			
			</div>
		</div>
	
	</div>

</body>
</html>